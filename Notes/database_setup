CREATE TABLE Foods (
  foodid  integer   PRIMARY KEY, 
  fname   varchar(30) NOT NULL,
  hall    varchar(20),
  votes   integer   DEFAULT 0,  
  date    date,   
  meal    char(1),  
  comments  integer[] 
);