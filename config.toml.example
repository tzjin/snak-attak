[copy to config.toml, fill out fields, and DO NOT COMMIT]

[general]
public_path   = "public"
template_path = "views"

[cookie]
mac_secret = "secret string which has over 256-bit entropy"
secure     = false

[csrf]
key    = "csrf_token"
cookie = "XSRF-TOKEN"
header = "X-XSRF-TOKEN"
